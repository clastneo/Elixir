# atom은 어떤 이름을 나타내는 상수.
# 앞에 콜론(:)을 쓰고 유효한 문자나 엘릭서 연산자를 붙이면 아톰이 된다.
# 아톰으로 사용할 수 있는 문자는 UTF-8, 숫자, _, @ 등이 있다.
# 느낌표나 물음표로 끝낼 수도 있고, 큰따옴표 사이에 임의의 문자를 넣어 아톰을 만들 수도 있다.

:fred
:is_binary?
:var@2
:===
:"func/3"
:"long john silver"

# 아톰의 값은 아톰의 이름과 같다(enum?)
# 아톰은 값에 이름표를 달기 위해 사용한다.
